[project]
name = "backend"
version = "0.1.0"
description = "Codecast Workflow Engine — FastAPI backend"
authors = [
    { name = "mehta", email = "anmol.mehta018@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "alembic>=1.19.1",
    "bcrypt==4.2.1",
    "fastapi[standard]>=0.141.1",
    "passlib[bcrypt]>=1.7.4",
    "pydantic>=2",
    "python-jose[cryptography]>=3.5.0",
    "python-multipart>=0.0.32",
    "sqlalchemy[asyncio]>=2.0",
]

[tool.uv]
package = false
